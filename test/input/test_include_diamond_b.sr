// Right branch of the diamond include graph.
// This file also includes test_include_diamond_common.sr.
// The parser must handle this duplicate include without error.
#include "test_include_diamond_common.sr"

struct MeshLight
{
    uint   meshId;
    float3 emissiveColor;
    float  emissiveIntensity;
    uint   triangleOffset;
    uint   triangleCount;
};
